services:
  # Redis (Celery broker + cache)
  redis:
    image: redis:7-alpine
    command: redis-server --appendonly yes
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped

  # PostgreSQL (authoritative app database for Docker deployments)
  postgres:
    image: postgres:16-alpine
    environment:
      - POSTGRES_DB=${POSTGRES_DB:-stockscanner}
      - POSTGRES_USER=${POSTGRES_USER:-stockscanner}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD:-stockscanner}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-stockscanner} -d ${POSTGRES_DB:-stockscanner}"]
      interval: 10s
      timeout: 5s
      retries: 10
    restart: unless-stopped

  # FastAPI Backend
  backend:
    build:
      context: .
      dockerfile: backend/Dockerfile
    environment:
      - DATABASE_URL=postgresql://${POSTGRES_USER:-stockscanner}:${POSTGRES_PASSWORD:-stockscanner}@postgres:5432/${POSTGRES_DB:-stockscanner}
      - POSTGRES_DB=${POSTGRES_DB:-stockscanner}
      - POSTGRES_USER=${POSTGRES_USER:-stockscanner}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD:-stockscanner}
      - REDIS_HOST=redis
      - REDIS_PORT=6379
      - CELERY_BROKER_URL=redis://redis:6379/0
      - CELERY_RESULT_BACKEND=redis://redis:6379/1
      - API_HOST=0.0.0.0
      - API_PORT=8000
      # CORS - configurable for production, defaults to localhost for dev
      - CORS_ORIGINS=${CORS_ORIGINS:-http://localhost,http://localhost:80,http://frontend}
      # API Keys (from .env or .env.docker)
      - ALPHA_VANTAGE_API_KEY=${ALPHA_VANTAGE_API_KEY:-}
      - GEMINI_API_KEY=${GEMINI_API_KEY:-}
      - GOOGLE_API_KEY=${GOOGLE_API_KEY:-}
      - GROQ_API_KEY=${GROQ_API_KEY:-}
      - GROQ_API_KEYS=${GROQ_API_KEYS:-}
      - DEEPSEEK_API_KEY=${DEEPSEEK_API_KEY:-}
      - TOGETHER_API_KEY=${TOGETHER_API_KEY:-}
      - OPENROUTER_API_KEY=${OPENROUTER_API_KEY:-}
      - TAVILY_API_KEY=${TAVILY_API_KEY:-}
      - SERPER_API_KEY=${SERPER_API_KEY:-}
      # XUI Twitter ingestion
      - XUI_ENABLED=${XUI_ENABLED:-true}
      - XUI_CONFIG_PATH=${XUI_CONFIG_PATH:-/app/data/xui-reader/config.toml}
      - XUI_PROFILE=${XUI_PROFILE:-default}
      - XUI_LIMIT_PER_SOURCE=${XUI_LIMIT_PER_SOURCE:-50}
      - XUI_NEW_ONLY=${XUI_NEW_ONLY:-true}
      - XUI_CHECKPOINT_MODE=${XUI_CHECKPOINT_MODE:-auto}
      - XUI_BRIDGE_ENABLED=${XUI_BRIDGE_ENABLED:-true}
      - XUI_BRIDGE_ALLOWED_ORIGINS=${XUI_BRIDGE_ALLOWED_ORIGINS:-http://localhost:80,http://127.0.0.1:80,http://localhost:5173,http://127.0.0.1:5173}
      - XUI_BRIDGE_CHALLENGE_TTL_SECONDS=${XUI_BRIDGE_CHALLENGE_TTL_SECONDS:-120}
      - XUI_BRIDGE_MAX_COOKIES=${XUI_BRIDGE_MAX_COOKIES:-300}
      - TWITTER_REQUEST_DELAY=${TWITTER_REQUEST_DELAY:-5.0}
      # LLM Routing (optional overrides)
      - LLM_DEFAULT_PROVIDER=${LLM_DEFAULT_PROVIDER:-groq}
      - LLM_CHATBOT_MODEL=${LLM_CHATBOT_MODEL:-}
      - LLM_RESEARCH_MODEL=${LLM_RESEARCH_MODEL:-}
      - LLM_FALLBACK_ENABLED=${LLM_FALLBACK_ENABLED:-true}
    volumes:
      - ./data:/app/data
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/readyz"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    stop_grace_period: 30s
    restart: unless-stopped

  # Celery Worker - General Queue
  celery-worker:
    build:
      context: .
      dockerfile: backend/Dockerfile
    command: celery -A app.celery_app worker --pool=prefork --loglevel=info --concurrency=2 -Q celery -n general@%h
    environment:
      # Set HOME for yfinance cache (writes to /app/data/.cache/py-yfinance)
      - HOME=/app/data
      - TZ=${CELERY_TIMEZONE:-America/New_York}
      - CELERY_TIMEZONE=${CELERY_TIMEZONE:-America/New_York}
      - DATABASE_URL=postgresql://${POSTGRES_USER:-stockscanner}:${POSTGRES_PASSWORD:-stockscanner}@postgres:5432/${POSTGRES_DB:-stockscanner}
      - POSTGRES_DB=${POSTGRES_DB:-stockscanner}
      - POSTGRES_USER=${POSTGRES_USER:-stockscanner}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD:-stockscanner}
      - REDIS_HOST=redis
      - REDIS_PORT=6379
      - CELERY_BROKER_URL=redis://redis:6379/0
      - CELERY_RESULT_BACKEND=redis://redis:6379/1
      # API Keys
      - ALPHA_VANTAGE_API_KEY=${ALPHA_VANTAGE_API_KEY:-}
      - GEMINI_API_KEY=${GEMINI_API_KEY:-}
      - GOOGLE_API_KEY=${GOOGLE_API_KEY:-}
      - GROQ_API_KEY=${GROQ_API_KEY:-}
      - GROQ_API_KEYS=${GROQ_API_KEYS:-}
      - DEEPSEEK_API_KEY=${DEEPSEEK_API_KEY:-}
      - TOGETHER_API_KEY=${TOGETHER_API_KEY:-}
      - OPENROUTER_API_KEY=${OPENROUTER_API_KEY:-}
      - TAVILY_API_KEY=${TAVILY_API_KEY:-}
      - SERPER_API_KEY=${SERPER_API_KEY:-}
      # XUI Twitter ingestion
      - XUI_ENABLED=${XUI_ENABLED:-true}
      - XUI_CONFIG_PATH=${XUI_CONFIG_PATH:-/app/data/xui-reader/config.toml}
      - XUI_PROFILE=${XUI_PROFILE:-default}
      - XUI_LIMIT_PER_SOURCE=${XUI_LIMIT_PER_SOURCE:-50}
      - XUI_NEW_ONLY=${XUI_NEW_ONLY:-true}
      - XUI_CHECKPOINT_MODE=${XUI_CHECKPOINT_MODE:-auto}
      - TWITTER_REQUEST_DELAY=${TWITTER_REQUEST_DELAY:-5.0}
      # LLM Routing
      - LLM_DEFAULT_PROVIDER=${LLM_DEFAULT_PROVIDER:-groq}
      - LLM_CHATBOT_MODEL=${LLM_CHATBOT_MODEL:-}
      - LLM_RESEARCH_MODEL=${LLM_RESEARCH_MODEL:-}
      - LLM_FALLBACK_ENABLED=${LLM_FALLBACK_ENABLED:-true}
    volumes:
      - ./data:/app/data
    depends_on:
      backend:
        condition: service_started
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    stop_grace_period: 30s
    restart: unless-stopped

  # Celery Worker - Data Fetch Queue (serialized; keeps xui-reader single-writer)
  celery-datafetch:
    build:
      context: .
      dockerfile: backend/Dockerfile
    command: celery -A app.celery_app worker --pool=prefork --loglevel=info --concurrency=1 -Q data_fetch -n datafetch@%h
    environment:
      # Set HOME for yfinance cache (writes to /app/data/.cache/py-yfinance)
      - HOME=/app/data
      - TZ=${CELERY_TIMEZONE:-America/New_York}
      - CELERY_TIMEZONE=${CELERY_TIMEZONE:-America/New_York}
      - DATABASE_URL=postgresql://${POSTGRES_USER:-stockscanner}:${POSTGRES_PASSWORD:-stockscanner}@postgres:5432/${POSTGRES_DB:-stockscanner}
      - POSTGRES_DB=${POSTGRES_DB:-stockscanner}
      - POSTGRES_USER=${POSTGRES_USER:-stockscanner}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD:-stockscanner}
      - REDIS_HOST=redis
      - REDIS_PORT=6379
      - CELERY_BROKER_URL=redis://redis:6379/0
      - CELERY_RESULT_BACKEND=redis://redis:6379/1
      # API Keys
      - ALPHA_VANTAGE_API_KEY=${ALPHA_VANTAGE_API_KEY:-}
      - GEMINI_API_KEY=${GEMINI_API_KEY:-}
      - GOOGLE_API_KEY=${GOOGLE_API_KEY:-}
      - GROQ_API_KEY=${GROQ_API_KEY:-}
      - GROQ_API_KEYS=${GROQ_API_KEYS:-}
      - DEEPSEEK_API_KEY=${DEEPSEEK_API_KEY:-}
      - TOGETHER_API_KEY=${TOGETHER_API_KEY:-}
      - OPENROUTER_API_KEY=${OPENROUTER_API_KEY:-}
      - TAVILY_API_KEY=${TAVILY_API_KEY:-}
      - SERPER_API_KEY=${SERPER_API_KEY:-}
      # XUI Twitter ingestion
      - XUI_ENABLED=${XUI_ENABLED:-true}
      - XUI_CONFIG_PATH=${XUI_CONFIG_PATH:-/app/data/xui-reader/config.toml}
      - XUI_PROFILE=${XUI_PROFILE:-default}
      - XUI_LIMIT_PER_SOURCE=${XUI_LIMIT_PER_SOURCE:-50}
      - XUI_NEW_ONLY=${XUI_NEW_ONLY:-true}
      - XUI_CHECKPOINT_MODE=${XUI_CHECKPOINT_MODE:-auto}
      - TWITTER_REQUEST_DELAY=${TWITTER_REQUEST_DELAY:-5.0}
      # LLM Routing
      - LLM_DEFAULT_PROVIDER=${LLM_DEFAULT_PROVIDER:-groq}
      - LLM_CHATBOT_MODEL=${LLM_CHATBOT_MODEL:-}
      - LLM_RESEARCH_MODEL=${LLM_RESEARCH_MODEL:-}
      - LLM_FALLBACK_ENABLED=${LLM_FALLBACK_ENABLED:-true}
    volumes:
      - ./data:/app/data
    depends_on:
      backend:
        condition: service_started
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    stop_grace_period: 30s
    restart: unless-stopped

  # Celery Worker - User Scans Queue (isolated from maintenance jobs)
  celery-userscans:
    build:
      context: .
      dockerfile: backend/Dockerfile
    command: celery -A app.celery_app worker --pool=prefork --loglevel=info --concurrency=2 -Q user_scans -n userscans@%h
    environment:
      # Set HOME for yfinance cache (writes to /app/data/.cache/py-yfinance)
      - HOME=/app/data
      - TZ=${CELERY_TIMEZONE:-America/New_York}
      - CELERY_TIMEZONE=${CELERY_TIMEZONE:-America/New_York}
      - DATABASE_URL=postgresql://${POSTGRES_USER:-stockscanner}:${POSTGRES_PASSWORD:-stockscanner}@postgres:5432/${POSTGRES_DB:-stockscanner}
      - POSTGRES_DB=${POSTGRES_DB:-stockscanner}
      - POSTGRES_USER=${POSTGRES_USER:-stockscanner}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD:-stockscanner}
      - REDIS_HOST=redis
      - REDIS_PORT=6379
      - CELERY_BROKER_URL=redis://redis:6379/0
      - CELERY_RESULT_BACKEND=redis://redis:6379/1
      # API Keys
      - ALPHA_VANTAGE_API_KEY=${ALPHA_VANTAGE_API_KEY:-}
      - GEMINI_API_KEY=${GEMINI_API_KEY:-}
      - GOOGLE_API_KEY=${GOOGLE_API_KEY:-}
      - GROQ_API_KEY=${GROQ_API_KEY:-}
      - GROQ_API_KEYS=${GROQ_API_KEYS:-}
      - DEEPSEEK_API_KEY=${DEEPSEEK_API_KEY:-}
      - TOGETHER_API_KEY=${TOGETHER_API_KEY:-}
      - OPENROUTER_API_KEY=${OPENROUTER_API_KEY:-}
      - TAVILY_API_KEY=${TAVILY_API_KEY:-}
      - SERPER_API_KEY=${SERPER_API_KEY:-}
      # XUI Twitter ingestion
      - XUI_ENABLED=${XUI_ENABLED:-true}
      - XUI_CONFIG_PATH=${XUI_CONFIG_PATH:-/app/data/xui-reader/config.toml}
      - XUI_PROFILE=${XUI_PROFILE:-default}
      - XUI_LIMIT_PER_SOURCE=${XUI_LIMIT_PER_SOURCE:-50}
      - XUI_NEW_ONLY=${XUI_NEW_ONLY:-true}
      - XUI_CHECKPOINT_MODE=${XUI_CHECKPOINT_MODE:-auto}
      - TWITTER_REQUEST_DELAY=${TWITTER_REQUEST_DELAY:-5.0}
      # LLM Routing
      - LLM_DEFAULT_PROVIDER=${LLM_DEFAULT_PROVIDER:-groq}
      - LLM_CHATBOT_MODEL=${LLM_CHATBOT_MODEL:-}
      - LLM_RESEARCH_MODEL=${LLM_RESEARCH_MODEL:-}
      - LLM_FALLBACK_ENABLED=${LLM_FALLBACK_ENABLED:-true}
    volumes:
      - ./data:/app/data
    depends_on:
      backend:
        condition: service_started
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    stop_grace_period: 30s
    restart: unless-stopped

  # Celery Beat Scheduler
  celery-beat:
    build:
      context: .
      dockerfile: backend/Dockerfile
    command: sh -c "rm -f /app/data/celerybeat-schedule && celery -A app.celery_app beat --loglevel=info --schedule=/app/data/celerybeat-schedule"
    environment:
      - TZ=${CELERY_TIMEZONE:-America/New_York}
      - CELERY_TIMEZONE=${CELERY_TIMEZONE:-America/New_York}
      - DATABASE_URL=postgresql://${POSTGRES_USER:-stockscanner}:${POSTGRES_PASSWORD:-stockscanner}@postgres:5432/${POSTGRES_DB:-stockscanner}
      - POSTGRES_DB=${POSTGRES_DB:-stockscanner}
      - POSTGRES_USER=${POSTGRES_USER:-stockscanner}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD:-stockscanner}
      - REDIS_HOST=redis
      - REDIS_PORT=6379
      - CELERY_BROKER_URL=redis://redis:6379/0
      - CELERY_RESULT_BACKEND=redis://redis:6379/1
      # XUI Twitter ingestion
      - XUI_ENABLED=${XUI_ENABLED:-true}
      - XUI_CONFIG_PATH=${XUI_CONFIG_PATH:-/app/data/xui-reader/config.toml}
      - XUI_PROFILE=${XUI_PROFILE:-default}
      - XUI_LIMIT_PER_SOURCE=${XUI_LIMIT_PER_SOURCE:-50}
      - XUI_NEW_ONLY=${XUI_NEW_ONLY:-true}
      - XUI_CHECKPOINT_MODE=${XUI_CHECKPOINT_MODE:-auto}
      - TWITTER_REQUEST_DELAY=${TWITTER_REQUEST_DELAY:-5.0}
    volumes:
      - ./data:/app/data
    depends_on:
      backend:
        condition: service_started
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    stop_grace_period: 15s
    restart: unless-stopped

  # PostgreSQL backup - daily pg_dump to the shared data volume
  db-backup:
    image: postgres:16-alpine
    entrypoint: /bin/sh
    environment:
      - PGPASSWORD=${POSTGRES_PASSWORD:-stockscanner}
      - POSTGRES_DB=${POSTGRES_DB:-stockscanner}
      - POSTGRES_USER=${POSTGRES_USER:-stockscanner}
    command:
      - -c
      - |
        is_valid_backup() {
          [ -s "$$1" ]
        } && \
        prune_backups() {
          KEEP_PATH="$$1"
          mkdir -p /app/data/backups
          find /app/data/backups -maxdepth 1 -type f \
            \( -name 'stockscanner_*.dump' -o -name '.stockscanner_backup_in_progress.dump' \) \
            ! -path "$$KEEP_PATH" \
            -exec rm -f {} +
        } && \
        EXISTING_KEEP="" && \
        for CANDIDATE in $$(ls -1t /app/data/backups/stockscanner_*.dump 2>/dev/null); do \
          if is_valid_backup "$$CANDIDATE"; then \
            EXISTING_KEEP="$$CANDIDATE"; \
            break; \
          fi; \
        done && \
        if [ -n "$$EXISTING_KEEP" ]; then \
          prune_backups "$$EXISTING_KEEP"; \
          echo "Retained existing valid backup: $$EXISTING_KEEP"; \
        fi && \
        echo "Backup service started. Running every 24h." && \
        sleep 300 && \
        while true; do
          mkdir -p /app/data/backups
          TMP_DEST="/app/data/backups/.stockscanner_backup_in_progress.dump"
          DEST="/app/data/backups/stockscanner_$$(date +%Y%m%d_%H%M%S).dump"
          rm -f "$$TMP_DEST"
          pg_dump \
            --host=postgres \
            --username="$${POSTGRES_USER}" \
            --dbname="$${POSTGRES_DB}" \
            --format=custom \
            --file="$$TMP_DEST" 2>&1
          if [ $$? -eq 0 ] && is_valid_backup "$$TMP_DEST"; then
            mv "$$TMP_DEST" "$$DEST"
            prune_backups "$$DEST"
            echo "$$(date): Backup OK -> $$DEST ($$(du -h $$DEST | cut -f1)); retained newest valid backup only"
          else
            rm -f "$$TMP_DEST"
            echo "$$(date): Backup FAILED or validation failed"
          fi
          sleep 86400
        done
    volumes:
      - ./data:/app/data
    depends_on:
      postgres:
        condition: service_healthy
    restart: unless-stopped

  # Frontend (React served by nginx)
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      args:
        - VITE_API_URL=/api
    ports:
      - "80:80"
    depends_on:
      - backend
    restart: unless-stopped

volumes:
  redis_data:
  postgres_data:
